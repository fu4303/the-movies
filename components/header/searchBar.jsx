import { Input } from "@chakra-ui/react";

const SearchBar = () => {
  return (
    <>
      <Input placeholder="Search..." w={{ base: "full", lg: "25%" }} />
    </>
  );
};

export default SearchBar;
